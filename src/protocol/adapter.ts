import type { IncomingMessage, OutgoingMessage } from './types'

export const adaptIncomingMessage = (message: IncomingMessage): IncomingMessage =>
  message

export const adaptOutgoingMessage = (message: OutgoingMessage): OutgoingMessage =>
  message
